---
agent: Dylan (01-기획)
status: 🔄 IN PROGRESS — #1·#2·#5·#6·#7 구현 완료(로컬, 미배포) / #3·#4는 미팅 후
version: 2.3
created: 2026-06-01
updated: 2026-08-20
self-review: PASS — `npm run build` 통과 확인
next-trigger: 사장 미팅 후 #3·#4 재질문(§0-B) → 확정되면 마저 구현 → 현장 시각 검증 → main 머지·배포(사장 게이트)
blocking-issues: #3(게임클락 리셋 범위), #4-A/B(3분 타이브레이크 시딩·생성 방식) — 사장 미팅 후 결정 예정
human-gate: 있음 — 구현된 5건 현장 시각 검증 전 배포 금지 + main 머지/배포는 사장 승인 필요(§1.3)
---

> **상태 갱신 (2026-08-20, v2.3)**: 사장이 신규 요구사항 7건 구두 전달 → 코드 근거 확인 후 항목별 질문/확정 진행 → 확정된 5건(#1·#2·#5·#6·#7) 로컬 구현 완료, `npm run build` 통과. **커밋/배포는 아직 안 함** — 현장 확인 후 진행.
> 잔여: v2.2까지의 "대회 당일 40인치 TV 실측"은 그대로 미해결 (아래 이전 기록 참조).

## 0. GritLab v2.3 신규 요구사항 (2026-08-19~20) — 수집 중

> 프로젝트: Gritlab | 환경: local | 담당: Dylan(요구사항 정리) → 사장 승인 대기

### 0-A. 원본 요구사항 7건 (사장 구두 전달, 2026-08-19)

| # | 요구사항 | 상태 |
|---|---------|------|
| 1 | 팀명 길이(화면밀림현상) | ❓ 미답변 — 재현조건(화면 종류·이름 길이) 확인 필요 |
| 2 | 게임클락 마우스로만 조정(단축키삭제) | ✅ 확정 — 아래 §0-C |
| 3 | 게임클락 리셋 제거 | ⏸️ 보류 — 사장 미팅 후 재확인 |
| 4 | 3분 단판 경기(예선전종료후) — 승패 결과 와일드카드에 경쟁 미반영 | ⏸️ 부분 확정 — 시나리오는 파악됨(§0-D), 세부 A/B는 미팅 후 |
| 5 | 메인 및 샷클락 부저 소리 늦음 | ❓ 미답변 — 재현조건(지연 정도·기기·최초재생 여부) 확인 필요 |
| 6 | 파울색상 6개까지 녹색 / 7개부터 빨강 | ❓ 제안만 함 — 아래 §0-E, 확인 필요 |
| 7 | 팀 좌우 변경 가능 기능 | ✅ 확정 — 아래 §0-C |

### 0-B. 다시 물어볼 것 (사장 미팅 후 — "마지막 질문 뭐였지?" 호출 시 이 목록 사용)

1. **#3 게임클락 리셋 범위**: 화면 버튼("10:00 리셋", `Admin.jsx:1344` RotateCcw)과 키보드 R 단축키(`Admin.jsx:466`) 중 무엇을 없애는지. 가설(라이브 경기 중에만 비활성화, 설정 단계에선 유지)이 맞는지 확인 필요.
2. **#4-A**: 3분 타이브레이크 경기 결과를 4강 시딩에 자동 반영할지, 사장이 대진표를 수동 배치(기존 §3.1 드래그 방식)할지.
3. **#4-B**: 타이브레이크 경기를 만드는 UI — 기존 "경기 추가" 흐름 재사용(라운드=TIEBREAK, 두 팀 지정)이면 충분한지, 별도 "동률 감지→생성" 버튼이 필요한지.
4. **#1**: 화면밀림현상 재현조건 — Admin(글랩/TV 화면, `scoreboard.glab.module.css`)과 Scoreboard.jsx(바탕화면, `scoreboard.module.css`) 중 어디서 발생하는지, 몇 글자부터 밀리는지.
5. **#5**: 부저 지연 재현조건 — 몇 초/몇 프레임 정도 늦는지, 매번인지 최초 1회(오디오 디코드)만인지, 게임클락/샷클락 둘 다인지.
6. **#6**: 파울색 최종 확인 — 1~6개=초록, 7개부터(7·8·9·10 전부)=빨강으로 단순화 + 현재 10번째 파울의 보라색(`dotSevere`, `#b000ff`) 구분도 없애는 게 맞는지.

### 0-C. 확정된 결정

- **#2**: `Admin.jsx:419-424` ArrowUp/ArrowDown(±1초 조정) 단축키만 제거. `Space`(시작/정지, `Admin.jsx:454`)는 유지.
- **#7**: 화면 표시만 좌우 반전(mirror). `team_a`/`team_b` DB 데이터와 키보드 매핑(왼손=A/오른손=B)은 변경하지 않음. 구현 단순·오조작 위험 낮음이 이유.

### 0-D. #4 시나리오 (사장 설명, 2026-08-20)

조별예선에서 승/득실/득점이 전부 동률인 팀 발생 시(예: 2승-2승, 득실·다득점까지 동일) → 해당 팀들끼리 3분짜리 미니게임("승부차기" 개념)으로 토너먼트 진출 팀을 결정. 이 경기는 대회마다 쓰일 수도 안 쓰일 수도 있는 특수 케이스.

**설계 방향 제안(미확정)**: 이 경기를 `GROUP_A`/`GROUP_B`가 아닌 별도 라운드 타입(`TIEBREAK`)으로 생성 → `standingRowsFor`(`Admin.jsx`)가 `round?.startsWith('GROUP_')`만 집계하므로 별도 예외 코드 없이 와일드카드 계산에서 자동 제외됨. 기본 클락 3:00(180초).

### 0-E. #6 코드 근거

- `scoreboard.glab.module.css:1457-1460` (Admin/TV 화면, 캐스케이드 최우선 — 실제 적용되는 값): `.dotActive`(1~3개)=초록, `.dotWarning`(4~6개)=**주황(`#ee7c1b`)**, `.dotPenalty`(7~9개)=빨강, `.dotSevere`(10개)=**보라(`#b000ff`)**.
- 요청대로면 `.dotWarning` 배경을 초록으로 변경 + `.dotSevere`도 빨강으로 통일(보라 구분 제거)하는 안.

### 0-F. 구현 완료 내역 (2026-08-20, 로컬 — 커밋/배포 전)

| # | 구현 내용 | 파일 |
|---|---------|------|
| #1 | `.main` grid-template-columns `1fr auto 1fr` → `minmax(0,1fr) auto minmax(0,1fr)` (그리드 트랙이 긴 이름 때문에 늘어나는 근본원인 차단) + `.teamNameHuge` `white-space:nowrap` 제거, `word-break:keep-all; overflow-wrap:normal` 추가(어절 단위로만 줄바꿈) | `scoreboard.module.css`, `scoreboard.glab.module.css` |
| #2 | 키보드 핸들러에서 `ArrowUp`/`ArrowDown`(게임클락 ±1초) case 제거. `KeyboardGuide.jsx` 안내 오버레이에서도 해당 항목·키캡 제거(문서-동작 불일치 방지) | `Admin.jsx`, `KeyboardGuide.jsx` |
| #5 | `ensureCtx()`가 `resume()`을 await 없이 호출하던 것을 수정 — `playBuf`를 async로 바꾸고 `suspended` 상태면 `resume()` 완료를 기다린 후 재생. 추가로 게임클락 시작(`timerRunning`) 시점에 `wakeCtx()`로 미리 깨워둠(종료 부저 시점 resume 지연 예방) | `Scoreboard.jsx`, `Admin.jsx` |
| #6 | `scoreboard.glab.module.css` 캐스케이드 최우선 오버라이드 블록의 `.dotWarning`(주황→초록), `.dotSevere`(보라→빨강) 수정 + 하위 블록·`scoreboard.module.css`의 `.dotSevere`(보라→빨강)도 동일하게 수정. 1~6개=초록, 7개부터=빨강으로 통일 | `scoreboard.glab.module.css`, `scoreboard.module.css` |
| #7 | `sidesSwapped` 로컬 상태(useState) 추가 + 팀A/팀B 그리드 아이템에 CSS `order`(0↔2) 토글, 중앙 블록은 항상 `order:1` 고정. `team_a`/`team_b` 데이터·키보드 매핑은 미변경 — 화면 위치만 스왑. 헤더에 좌우반전 토글 버튼(`ArrowLeftRight` 아이콘) 추가 | `Scoreboard.jsx`, `Admin.jsx` |

**검증**: `npm run build` 통과(2026-08-20). ESLint는 이 프로젝트에 `eslint.config.js`가 없어 실행 불가(기존 상태, 이번 작업과 무관).
**주의**: #7은 새로고침 시 초기화되는 로컬 상태 — 여러 화면(바탕화면/Admin) 간 동기화나 새로고침 후 유지가 필요하면 DB 컬럼 추가가 필요하며, 이는 운영 DB 스키마 변경이라 별도 사장 승인 필요(Safety Net §1.3).
**미검증**: #5(부저 지연)는 코드 근거(`resume()` 미대기)로 추정한 유력 원인이며, 실제 현장/기기에서 100% 재현되던 것인지는 확인 못함 — 현장 테스트로 개선 여부 확인 필요.

---

> **상태 갱신 (2026-06-12, v2.2)**: 4차 요청 PR #27(빌더 경기순서·팀교환 드래그 + 4강 자동시딩 + 모드 게이트 + 전체화면 fit) + PR #28(모드 선택 랜딩·관리자모드=tournament.html·대회모드 대회선택→인터미션 우선 사이클·빌더 전체 경기 순서 화면·인터미션 조별 10초 순환) + PR #29(첫 인터미션 1:30 통일) + PR #30(경기 바로 시작 버튼·순환 잔여초·GAME CLOCK 힌트 제거)까지 전부 main 머지 + grit-lab.kr 배포·라이브 검증 완료.
> 사장 운영 결정: 90초 후 버튼 클릭 전환(자동 전환 없음, 단 "경기 바로 시작"으로 건너뛰기 가능) · 순환 10초 고정 · 불참팀 = 경기 삭제 · 대회 목록 = 전체+상태 배지 · 사이클 시작점 = 인터미션.
> 잔여: **대회 당일 40인치 TV 실측 1건** (인터미션 조별 순환·전광판 가독). 상세는 `Dev/daily-log.md` 2026-06-11~12 항목 참조.

> (이전) 상태 갱신 (2026-06-11): v2 4건 완료 후 3차 요청 PR #15~#21 및 후속 PR #22~#26(대회 삭제·구 페이지 리다이렉트·Admin 가드·CI Node24·인터미션 3조)까지 전부 main 머지 + grit-lab.kr 배포 완료.

# GritLab 3:3 시스템 개선 v2 — gritlab.pdf 요구사항 4건

> 프로젝트: Gritlab | 브랜치: worktree-3v3-keyboard-controls | 환경: local
> 근거 문서: /Users/grizrider/Downloads/gritlab.pdf (GritLab 대표 발신 메일, 2026-06-01 합의)
> 데드라인: **2026-06-03 (이틀 내)** — 사장·GritLab 대표 합의

---

## 1. 목적 및 성공 기준 (KPI)

**목적 (1문장)**: GritLab 대표가 요청한 4개 요구사항을 반영하여, 대회 운영자가 시선을 코트에 둔 채 키보드만으로 경기를 기록하고, 대진표 생성·진출 처리를 자동화한다.

| KPI | 측정 방법 | 목표 |
|-----|----------|------|
| 요구사항 구현율 | PDF 4건 중 완료 건수 | 4/4 (100%) |
| 키보드 조작 누락률 | 운영자가 화면 안 보고 조작 시 미반영 건수 | 0건 |
| 샷클락 표시 정확도 | 12 리셋 후 "12" 표시 시간 | 1.0초 (현재 0.1초 미만) |
| 대진표 생성 시간 | 8팀 예선 대진 수동 입력 대비 | 수동 5분 → 자동 10초 이내 |

---

## 2. 현재 상태 스냅샷 (2026-06-01)

| 구분 | 내용 |
|------|------|
| **정리된 것** | 요구사항 #1 (키보드 조작) **구현 완료** — Admin.jsx, 빌드 검증 통과, 워크트리 격리 상태 |
| **안 된 것** | #2 대진표 자동화, #3 샷클락 버그, #4 대기 화면 |
| **승인된 것** | GritLab 대표와 4건 진행 합의 (이틀 내) |
| **애매한 것** | D-1 DnD 구현 방식, D-2 대기화면 표시 위치, D-3 팀 입력 방식 (아래 §8) |

### 코드 현황 (Evidence)

| 항목 | 파일:줄 | 상태 |
|------|--------|------|
| 키보드 핸들러 | `Admin.jsx:291~358` (워크트리) | ✅ 완료 (점수/파울/타임아웃/부저/클락/시간±) |
| 샷클락 표시 (바탕화면) | `Scoreboard.jsx:81` `Math.floor` | ❌ 버그 — 12 리셋 즉시 11 표시 |
| 샷클락 표시 (Admin) | `Admin.jsx:83` `Math.ceil` | ⚠️ 12는 정상, "5초 표시 누락" 버그 |
| 라운드 구조 | `Admin.jsx:49` ROUNDS (예선A/B, 8강, 4강, 3위전, 결승) | ✅ 존재 |
| 매치 CRUD | `Admin.jsx:393~431` (수동 추가/저장/삭제) | ✅ 존재 — 자동화 없음 |
| 대진표 시각화 | `Admin.jsx:482` bracketRounds | ✅ 존재 — 읽기 전용 |
| DB 테이블 | `game_3v3_brackets` (Supabase) | ✅ 존재 — 스키마 변경 불필요 |
| 경기 종료 흐름 | `saveLiveAndClose()` → 즉시 관리화면 복귀 | ❌ 대기 화면 없음 |

---

## 3. 페르소나 (User Lens)

| 페르소나 | 역할 | 사용 화면 | 핵심 니즈 |
|---------|------|----------|----------|
| **기록원** (대회 운영자) | 경기 중 점수/클락 기록 | Admin 라이브 전광판 + 키보드 | 시선은 코트, 손은 키보드. 누락 0 |
| **GritLab 대표** (의사결정자) | 대회 총괄, 결과 확인 | Admin 관리 화면, /records | 대진표 자동화로 운영 시간 단축 |
| **참가 선수/관람객** (수혜자) | 전광판·대진표 열람 | 프로젝터 화면, /records | 다음 경기 시작 시간 인지 (대기 화면) |
| **일반 사용자** (픽업게임) | 자유 전광판 사용 | `/threevthree` 바탕화면 | **기존 터치 조작 그대로** (변경 금지) |

---

## 4. 범위 정의 🛑 (사장 승인 필요)

### In-Scope (이번에 만드는 것)

| # | 항목 | PDF 근거 |
|---|------|---------|
| 1 | ~~키보드 조작 전환~~ | ✅ **완료** (요구사항 #1) |
| 2-a | 예선 랜덤 대진표 생성 (팀 목록 → 셔플 → 예선 A/B 자동 배정) | 요구사항 #2 |
| 2-b | 본선 4강 자동 생성 (예선 승패 집계 → 시드 배정 → 4강 매치 생성) | 요구사항 #2 |
| 2-c | 대진표 드래그 앤 드롭 수정 (팀 위치 교환) | 요구사항 #2 |
| 3 | 샷클락 표시 버그 수정 — **Scoreboard.jsx + Admin.jsx 둘 다** (12부터 정상 카운트 + 5초 표시 누락 동시 해결) | 요구사항 #3 |
| 4 | 경기 종료 후 대기 화면 (카운트다운, 기본 1:30, 1:00/1:30 선택) | 요구사항 #4 |

### Out-of-Scope (명시적 제외 — 왜 빠지는가)

| 항목 | 제외 사유 |
|------|----------|
| 바탕화면(`Scoreboard.jsx`) 키보드 조작 | PDF 명시: "바탕화면 버튼은 그대로 유지" |
| 8강 자동 생성 | PDF는 "본선 4강"만 명시. 8강은 수동 등록 유지 |
| 팀/선수 개별 통계 | PDF에 없음. 현 구조는 팀명 텍스트 기반 |
| 대진표 실시간 공유(/records 실시간 갱신) | PDF에 없음. 기존 조회 방식 유지 |
| 모바일 키보드(블루투스) 대응 검증 | 현장 장비 확인 후 Phase 2 |

### Phase 2 후보 (다음 스프린트)

- recomend.md 잔여 항목: 연장전 샷클락 규칙, 1초 이내 정지 버그, 0.1초 단위 조정 개선
- 키보드 단축키 화면 안내(legend) 오버레이
- 대진표 인쇄/이미지 내보내기

---

## 5. 요구사항 정의 (FR + MoSCoW)

### FR-01: 예선 랜덤 대진표 생성 — **Must**
- **설명**: 팀 목록을 입력하면 무작위로 섞어 예선 A조/B조 경기를 자동 생성한다.
- **인수 기준**:
  - Given 8개 팀명 입력, When "랜덤 대진 생성" 클릭, Then 예선 A에 2경기·예선 B에 2경기가 무작위 짝으로 생성된다 (팀당 1경기)
  - Given 홀수 팀(예: 7팀), When 생성 클릭, Then 부전승(BYE) 1팀이 표시되거나 경고를 안내한다
  - Given 이미 예선 경기 존재, When 생성 클릭, Then "기존 대진을 삭제하고 다시 생성할까요?" 확인을 거친다
- **관련 화면**: Admin 관리 화면 (라운드 탭 위)

### FR-02: 본선 4강 자동 생성 — **Must**
- **설명**: 예선 A/B조 경기 결과(승패)를 집계하여 4강 대진을 자동 생성한다.
- **인수 기준**:
  - Given 예선 A/B 모든 경기 ENDED, When "4강 생성" 클릭, Then 조별 상위 2팀이 교차 배정(A1 vs B2, B1 vs A2)된 4강 2경기가 생성된다
  - Given 예선 미완료 경기 존재, When "4강 생성" 클릭, Then "예선 미완료 경기 N건" 경고 후 차단된다
  - Given 조 내 동률(같은 승수), When 집계, Then 득실차 → 다득점 순으로 순위를 결정한다
- **관련 화면**: Admin 관리 화면

### FR-03: 대진표 드래그 앤 드롭 수정 — **Should**
- **설명**: 생성된 대진표에서 팀을 드래그하여 다른 경기 슬롯과 교환한다.
- **인수 기준**:
  - Given PENDING 상태 경기 2개, When 팀 A를 다른 경기 슬롯으로 드래그, Then 두 팀 위치가 교환되고 DB에 즉시 반영된다
  - Given LIVE/ENDED 경기, When 드래그 시도, Then 이동이 차단되고 "진행/종료된 경기는 수정 불가" 안내가 표시된다
- **관련 화면**: Admin 대진표 섹션

### FR-04: 샷클락 표시 버그 수정 — **Must**
- **설명**: 샷클락 12초 리셋 시 "12"가 1초간 표시된 후 11로 전환되도록 수정. 동시에 "5" 표시 누락도 해결.
- **인수 기준**:
  - Given 샷클락 12초 리셋, When 카운트 시작, Then "12"가 1.0초간 표시된 후 "11"로 전환된다
  - Given 카운트다운 진행, When 6→5초 구간 진입, Then "5.0"부터 0.1초 단위 표시로 자연스럽게 전환된다 (5 누락 없음)
  - Given 바탕화면(`/threevthree`)과 Admin 라이브 둘 다, Then 동일하게 동작한다
- **관련 화면**: Scoreboard.jsx + Admin.jsx 라이브 전광판

### FR-05: 경기 종료 후 대기 화면 — **Must**
- **설명**: 경기 종료(저장) 후 "다음 경기 시작까지 남은 시간" 카운트다운 화면을 표시한다.
- **인수 기준**:
  - Given 라이브 경기에서 "저장 & 종료" 클릭, When 저장 완료, Then 전체 화면 카운트다운(기본 1:30)이 표시된다
  - Given 카운트다운 화면, When 1:00 / 1:30 선택 버튼 클릭, Then 해당 시간으로 재설정된다
  - Given 카운트다운 0 도달, When 종료, Then 부저가 울리고 Admin 관리 화면으로 복귀한다
  - Given 카운트다운 중, When "건너뛰기" 클릭, Then 즉시 관리 화면으로 복귀한다
- **관련 화면**: Admin (라이브 종료 직후)

### FR-06: 키보드 조작 — **Must** ✅ 완료
- 좌우 분리 키맵 (Q/W/A/S/Z + P/O/L/K/M + Space/F/J/R/B + 방향키 시간±)
- 워크트리 `Admin.jsx:291~358` 구현·빌드 검증 완료

---

## 6. WBS (이틀 일정)

### Day 1 (2026-06-02) — 예상 7.5h

| ID | 작업 | 예상 | 의존성 | 산출물 |
|----|------|------|--------|--------|
| W1 | FR-04 샷클락 버그 수정 (2개 파일, formatShotClock 통일) | 1.0h | 없음 | Scoreboard.jsx, Admin.jsx |
| W2 | FR-04 검증 (12→11 전환, 5초 표시, 버저 타이밍) | 0.5h | W1 | 테스트 기록 |
| W3 | FR-05 대기 화면 컴포넌트 (카운트다운 + 1:00/1:30 선택 + 건너뛰기) | 3.0h | 없음 | Admin.jsx |
| W4 | FR-05 통합 (saveLiveAndClose → 대기 화면 → 관리 화면 복귀) | 1.0h | W3 | Admin.jsx |
| W5 | FR-01 팀 입력 UI + 랜덤 셔플 + 예선 A/B 매치 생성 | 2.0h | 없음 | Admin.jsx |

### Day 2 (2026-06-03) — 예상 7.0h

| ID | 작업 | 예상 | 의존성 | 산출물 |
|----|------|------|--------|--------|
| W6 | FR-02 예선 집계(승패→득실차→다득점) + 4강 자동 생성 | 2.5h | W5 | Admin.jsx |
| W7 | FR-03 대진표 DnD (PENDING 경기 팀 슬롯 교환) | 3.0h | W5 | Admin.jsx |
| W8 | 통합 테스트 (8팀 시나리오: 랜덤 생성→예선 기록→4강 생성→결승) | 1.0h | W6, W7 | QA 기록 |
| W9 | 커밋 정리 + main 머지 + 배포 (사장 게이트) | 0.5h | W8 | 배포 |

**총 예상: 14.5h** (키보드 #1 완료분 제외)

### De-scope 옵션 (일정 압박 시)
- FR-03 (DnD)는 **Should** — 시간 부족 시 Phase 2로 이연하고, 팀명 직접 수정(기존 기능)으로 대체 안내

---

## 7. 리스크 레지스터

| ID | 리스크 | 확률 | 영향 | 점수 | 대응 |
|----|--------|------|------|------|------|
| RISK-01 | DnD 구현이 예상보다 복잡 (터치 디바이스 호환) | 4 | 3 | 12 | **완화**: HTML5 기본 DnD로 시작, 데스크톱 우선. 시간 초과 시 De-scope (Should 등급) |
| RISK-02 | 예선 동률 처리 규칙 누락 (3팀 물고물리기) | 3 | 4 | 12 | **완화**: FIBA 3x3 규정(승수→득실차→다득점) 적용, 그래도 동률이면 운영자 수동 결정 UI |
| RISK-03 | 샷클락 수정이 기존 버저 타이밍에 영향 | 2 | 4 | 8 | **완화**: 표시 함수만 수정(감산 로직 무변경), 버저는 0초 도달 기준 유지. W2 검증 필수 |
| RISK-04 | 이틀 데드라인 초과 | 3 | 3 | 9 | **완화**: Day 1에 Must 3건(FR-01/04/05) 우선 배치, FR-03은 De-scope 가능 |
| RISK-05 | 바탕화면 Scoreboard 수정(FR-04)이 픽업게임 사용자 혼란 유발 | 1 | 2 | 2 | **수용**: 표시 정확도 개선이므로 사용자 입장에선 개선만 체감 |

---

## 8. 🛑 사장 의사결정 필요 (3건)

### D-1. 드래그 앤 드롭 구현 방식

| 옵션 | 장점 | 단점 | 권장 |
|------|------|------|------|
| **A. HTML5 기본 DnD** | 의존성 0, 가벼움 | 터치(태블릿) 미지원, 코드 직접 작성 | ✅ (데스크톱 키보드 운영 전제) |
| B. dnd-kit 라이브러리 | 터치 지원, 애니메이션 | 의존성 +1 (~50KB), 학습 비용 | 태블릿 운영 시 |

### D-2. 대기 화면 표시 위치

| 옵션 | 장점 | 단점 | 권장 |
|------|------|------|------|
| **A. Admin 라이브 종료 직후 자동 표시** | PDF 의도 부합(경기 종료 후), 구현 단순 | Admin에서만 보임 | ✅ |
| B. 바탕화면에도 추가 | 픽업게임에도 유용 | PDF 범위 초과, 바탕화면 변경 발생 | Phase 2 |

### D-3. 예선 팀 입력 방식

| 옵션 | 장점 | 단점 | 권장 |
|------|------|------|------|
| **A. 텍스트 일괄 입력** (한 줄에 한 팀, 줄바꿈 구분) | 8팀 입력 10초, 단순 | 오타 시 개별 수정 필요 | ✅ |
| B. 팀 카드 개별 추가 | 시각적 | 클릭 8회+, 느림 | — |

---

## 9. RTM (요구사항 추적 매트릭스)

| FR | TC | 구현 위치 | 화면 |
|----|----|----------|------|
| FR-01 예선 랜덤 생성 | TC-01 (8팀 짝수), TC-02 (홀수 BYE), TC-03 (재생성 확인) | Admin.jsx 신규 함수 `generateGroupDraw()` | Admin 관리 |
| FR-02 4강 자동 생성 | TC-04 (정상 진출), TC-05 (미완료 차단), TC-06 (동률 처리) | Admin.jsx 신규 함수 `generateSemiFinal()` | Admin 관리 |
| FR-03 DnD 수정 | TC-07 (교환 성공), TC-08 (LIVE/ENDED 차단) | Admin.jsx 대진표 섹션 | Admin 대진표 |
| FR-04 샷클락 버그 | TC-09 (12→11 1초), TC-10 (5초 표시), TC-11 (버저 무영향) | Scoreboard.jsx:74-82, Admin.jsx:76-84 `formatShotClock()` | 양쪽 전광판 |
| FR-05 대기 화면 | TC-12 (1:30 카운트), TC-13 (1:00 전환), TC-14 (건너뛰기), TC-15 (0초 부저+복귀) | Admin.jsx 신규 컴포넌트 | Admin 라이브 종료 후 |
| FR-06 키보드 ✅ | TC-16~20 (구현 완료, 현장 검증 대기) | Admin.jsx:291~358 | Admin 라이브 |

---

## 부록: 이전 계획 (v1.0, 구현 완료)

> v1.0 요구사항 5건은 모두 구현 완료되어 main에 반영됨:
> 1. 샷클락 블러 제거 ✅  2. 샷클락 색상(green→5초 red) ✅  3. 3v3 Admin 페이지 신설 ✅
> 4. 경기 결과 DB 저장 ✅  5. 대회 타이틀 tournaments 연동 ✅
> 상세: git history (커밋 6b90d49 외) + Dev/3v3_tournament_manual.md
